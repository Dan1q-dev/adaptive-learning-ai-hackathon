# Frontend

React + Vite + TypeScript prototype for the adaptive learning service.

```bash
npm install
npm run dev
```

The app runs at `http://localhost:5173` and currently uses contract-compatible mock data from `src/mockApi.ts`.
